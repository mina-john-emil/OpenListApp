# Run with `flutter pub run ffigen --config ffigen.yaml`.
name: OpenListBackgroundServiceBindings
description: |
  Bindings for `src/openlist_background_service.h`.

  Regenerate bindings with `flutter pub run ffigen --config ffigen.yaml`.
output: 'lib/openlist_background_service_bindings_generated.dart'
headers:
  entry-points:
    - 'src/openlist_background_service.h'
  include-directives:
    - 'src/openlist_background_service.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
